// import bcrypt from 'bcrypt';

// const comparePassword = (plainPassword:string,password:string) =>{
//     const hashedPassword = bcrypt.compare(plainPassword,password,(err:string,result:any)=>{
//         if(result){
//             return result
//         }
//     })
//     console.log(hashedPassword);
    
// }
// export default comparePassword